# CogniCode

评估仓库「AI 原生程度」的方法学上下文：定义 AI 在代码库上完成任务的难易如何被测量、归因与改进。

## Language

**AI 原生程度**：
一个仓库对编程 agent 完成任务的友好程度，由维度模型测得。是本系统测量的核心属性。
_Avoid_: AI 友好度、AI-ready 程度、智能化程度

**维度模型**：
构成 AI 原生程度的正交维度集合。分数的地基，尚未定型（见 wayfinder 地图 #1）。

**基准任务**：
在目标仓库上动态生成的合成任务，交由执行者实际运行以测得成本与成败。任务跨仓库、跨语言可比。
_Avoid_: 测试用例、benchmark case

**执行者**：
被测量所用的编程 agent（CodeBuddy，dogfooding）。是被测对象的一部分而非工具本身。
_Avoid_: 评估器、runner

**测量协议**：
运行基准任务的规则集：重复采样次数、聚合统计量、异常运行处理。服务于分数可复现这一硬需求。

**静态归因信号**：
从仓库静态分析提取的语言无关信号，解释动态分数的成因并驱动改进建议。不进入总分。
_Avoid_: lint 规则、静态评分

**改进建议**：
由静态归因信号与任务失败样本推导出的、可验证的行动项（改后分数应可归因地变化）。

**试点仓库集**：
方法学开发期固定的一小组多样化试验仓库，用于难度校准、权重校准与可复现性验证。
