# CogniCode

仓库级 AI 原生程度评估与打分工具：用合成基准任务驱动真实编程 agent 执行，动态测量一个代码仓库对 AI 的友好程度。

## Language

### 评分模型

**AI 原生程度（AI-native readiness）**:
一个仓库被编程 agent 有效工作的容易程度，由六个概念维度聚合度量。
_Avoid_: AI 友好度、AI-readiness（口头可用，正式文本统一用本词）

**概念维度（dimension）**:
「AI 原生程度」的一根轴，对应一种独立的失败模式。共六个：可解性、变更安全性、效率、可导航性、环境可用性、可诊断性。
_Avoid_: 因素、指标（指标指维度下的具体测量项）

**双测量（dual measurement）**:
同一概念维度同时由动态测量（agent 执行任务）与静态测量（仓库信号分析）观测、聚合产分的结构。动静互为校验。
_Avoid_: 混合评分

**动态测量（dynamic measurement）**:
让真实编程 agent 在仓库上执行合成基准任务、从执行结果与过程信号中取值。
_Avoid_: 执行式评估

**静态测量（static measurement）**:
不经 agent 执行、直接从仓库文件与结构中提取信号取值。分两档：**产分信号**（进入维度分）与**归因信号**（解释失败、驱动建议）。

**静态信号集（static signal set）**:
静态测量的具体信号清单，分产分信号与归因信号两档。首发信号全部语言无关、可确定性提取（tree-sitter 或纯文件系统级），产出「存在/缺失/计数/密度」类客观事实，不做语义评分；产分信号首发仅覆盖可导航性与环境可用性两维度的静态面。

**代理上下文文档（agent context document）**:
专为编程 agent 准备的仓库上下文说明文件（AGENTS.md、CLAUDE.md、CONTEXT.md、.cursorrules 等）。可导航性静态面的产分信号之一，缺失是「找不到该找的代码」的静态诱因。

### 六个维度

**可解性（solvability）**:
在仓库语境下把修改/修复任务做对的能力。失败模式：解不出。

**变更安全性（safety）**:
做对的同时不破坏仓库既有行为的能力。失败模式：解出但炸了别处。

**效率（efficiency）**:
做对的过程成本（token/轮次/成本中位数）。失败模式：解出但昂贵。

**可导航性（navigability）**:
按自然语言描述定位到相关代码的能力。失败模式：找不到该找的代码。

**环境可用性（buildability）**:
从冷启动跑起构建与测试、获得验证反馈回路的能力。失败模式：跑不起验证回路。
_Avoid_: 可构建性、基建完备度

**可诊断性（diagnosability）**:
从失败症状定位根因的能力。失败模式：定位不了失败。

### 任务与验证

**基准任务集（benchmark suite）**:
针对目标仓库现场生成、现场执行的一组评估任务，天然免疫训练集污染。含环境探测与三类合成任务（检索、定位、修改/修复），对应「找代码/跑环境/查问题/改代码」。
_Avoid_: 基准实例、评测题

**合成任务（synthetic task）**:
有生成物（任务描述）与注入物（可控改动）的基准任务，共三类：检索（语义→位置）、定位（症状→根因）、修改/修复（描述→改动）。

**环境探测（probe）**:
零合成的基准任务：agent 从冷启动跑起构建与测试。探测先行，其结果决定合成任务的生成上限——基建缺失导致的任务降级是环境可用性的低分信号，不是评估失败。
_Avoid_: 构建任务、构建类任务

**F2P / P2P**:
执行式验证的双闸：改动前失败、改动后通过的测试（FAIL_TO_PASS）判定解出；改动前后都须通过的既有测试（PASS_TO_PASS）判定未破坏。
_Avoid_: 单独称「测试通过率」时不区分两闸

**注入点（injection point）**:
合成任务时在仓库中埋入的可控改动（bug、缺失等），必须被既有测试抓住（F2P 成立）。定位类任务的验收对照物。

**判卷权（verdict authority）**:
对错判定只属于 harness 侧客观判定（位置匹配、测试红绿、退出码）；agent 的结构化自报告仅约定交卷格式，不参与判定。
_Avoid_: 让模型自评

**难度元数据（difficulty metadata）**:
任务生成时由固定参考配置实测通过率标注的难度记录。只用于报告呈现与异常任务剔除，评分不做难度加权。

### 测量协议

**结果类别（outcome category）**:
一次任务运行的判卷归类，五类互斥：success（判卷通过）、fail_incorrect（跑完但未通过，含破坏既有行为）、fail_budget（触及轮次或时间上限）、fail_env（harness 侧环境故障）。前三类计入统计，fail_env 剔除并重跑。
_Avoid_: 把超时/跑飞当作异常数据丢弃（它是「解不出」的真实信号）

**采样次数（sample count, k）**:
同一任务独立重复执行的次数，固定取值、不自适应停止。agent 的随机性由重复采样与区间估计消化，不依赖温度控制。
_Avoid_: 自适应采样（停止偏倚，伤可复现性）

**运行环境快照（run snapshot）**:
随分数一并发布的执行环境记录：模型与 agent 版本、采样次数、任务量、超时与轮次上限。缺少快照的分数不可比较。

### 评分聚合与建议生成

**软横比（soft cross-repo comparison）**:
总分以绝对分（附区间）给出，跨仓库排序仅作软参考，不做硬承诺；建议生成只承诺本仓库纵向改善可验证。
_Avoid_: 把跨仓库排序当作硬可比的事实断言

**可重测声明（re-measurability claim）**:
每条改进建议附带的「锚定信号 + 预期方向 + 可重测」声明：改后重跑同一套任务与静态提取，用区间判定分数是否向预期方向移动。
_Avoid_: 只给建议不给可验证承诺

**报告（report）**:
本地 CLI 的输出物，分两层——终端打印「总览 → 维度明细 → 建议清单」一屏摘要，全量落盘 `cognicode-report.md`（含信号明细、失败任务证据、运行环境快照、权重敏感性输出）。纯文本、无 ANSI 色块，中文正文、术语括注英文。
_Avoid_: 只输出到终端不留档（可重测声明要求跨轮比较有锚点）

**报告架构版本（report-schema）**:
随运行环境快照发布的报告格式版本号 + 权重版本（等权 1/6 版本化）。跨轮比较先校验 schema 兼容，报告头摘要行显示 schema 号。
_Avoid_: 把不同 schema 下的分数直接比大小

**校准（calibration）**:
权重体系的首发设定与验证程序。重新定义为不拟合权重：① 方向验证——harness 落地后用语料方向预期（#9）核验测量是否方向正确，悖离则回修测量而非调权重；② 敏感性分析——每次打分随分数报告一并输出。
_Avoid_: 数据拟合权重（5 席 + 无 ground truth 必过拟合）

**权重敏感性分析（weight sensitivity analysis）**:
对六维权重做确定性网格扰动（每维 {0.5,0.75,1,1.25,1.5}×1/6 重归一化，全枚举）+ Dirichlet 采样补网，判定总分点值带宽（≤0.05）与语料方向排序稳定性。纯解析，无需实测分。
_Avoid_: 权重敏感性带宽（随报告附）与维度分/建议优先级混淆（后者与权重无关）
